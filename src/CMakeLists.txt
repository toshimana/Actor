CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT( libActor )

# Boost
FIND_PACKAGE( Boost REQUIRED COMPONENTS )
IF ( Boost_FOUND )
  INCLUDE_DIRECTORIES( ${Boost_INCLUDE_DIR} )
ENDIF()

# Target
FILE( GLOB SRCS "*.cpp" )
ADD_LIBRARY( libActor STATIC ${SRCS} )
